{
	"compilerOptions": {
		// required, configurable
		"rootDirs": ["src", "../shared/src"],
		"outDir": "out",

		// optional, non-configurable
		"paths": {
			"@shared/*": ["../../shared/src/*"]
		}
	},
	"include": ["**/*", "../shared/**/*"],
	"extends": "../../tsconfig.json"
}
